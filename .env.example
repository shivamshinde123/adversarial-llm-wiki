# Required for all LLM calls (compile, debate, lint)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# No additional API keys needed.
# --auto mode uses DuckDuckGo for web search (no API key required).
