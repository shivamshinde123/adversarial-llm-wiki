"""Auto Research Agent — searches the web and feeds content to the compiler."""


def run_research(topic: str, pro_stance: str | None, con_stance: str | None, topic_dir) -> None:
    """Search the web for both stances and compile wikis directly.

    Args:
        topic: Topic name.
        pro_stance: Optional custom description of the pro stance.
        con_stance: Optional custom description of the con stance.
        topic_dir: Path to the topic root directory.
    """
    raise NotImplementedError("Auto Research Agent not yet implemented (Stage 3).")
